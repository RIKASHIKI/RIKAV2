version: '3.8'
services:
  rika-bot:
    build: .
    ports:
      - "5000:5000"
      - "8082:8082"
    environment:
      - NODE_ENV=production
    volumes:
      - ./session:/app/session
      - ./media:/app/media
    restart: unless-stopped
